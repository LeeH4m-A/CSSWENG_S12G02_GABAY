// This folder is for reusable logic that runs before a route handler

// For example


// router.post('/dashboard', checkAuth, validateInput, dashboardController);
//                              ^^          ^^          // Controller would handle the "push" to database
